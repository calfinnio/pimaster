---

- name: Create project directory
  file:
    path: ~/pimaster
    state: directory
  tags: [ 'git_repos']

- name: Git clone repo
  command:
  args:
    chdir: ~/pimaster
  tags: [ 'git_repos']   